apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: blog
spec:
  tls:
  - hosts:
      - axe.al
  rules:
  - host: axe.al
    http:
      paths:
      - backend:
          serviceName: blog
          servicePort: 8080